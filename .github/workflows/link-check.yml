name: Link Check

on:
  # Only run on schedule and manual trigger
  # Disable automatic runs until repository is mature
  workflow_dispatch:
  schedule:
    # Run weekly on Monday at 00:00 UTC
    - cron: '0 0 * * 1'

jobs:
  link-check:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Check links in markdown files
      uses: gaurav-nelson/github-action-markdown-link-check@v1
      continue-on-error: true
      with:
        use-quiet-mode: 'yes'
        use-verbose-mode: 'no'
        config-file: '.github/workflows/link-check-config.json'

    # Disabled: Will create too many issues with placeholder links
    # Uncomment when repository links are finalized
    # - name: Create issue if links are broken
    #   if: failure()
    #   uses: actions/github-script@v6
    #   with:
    #     script: |
    #       github.rest.issues.create({
    #         owner: context.repo.owner,
    #         repo: context.repo.repo,
    #         title: 'Broken links detected in documentation',
    #         body: 'The link checker found broken links. Please review the workflow logs.',
    #         labels: ['documentation', 'bug']
    #       })
